package io.github.symmetricdevs.supercritical.api.nuclear.ecs

/**
 * Singleton data stored in a [World].
 *
 * Resources are ideal for data that is not associated with a specific entity
 * (e.g. a geometry cache, a hatch-inventory bridge, or a temporary solver buffer).
 */
interface Resource
